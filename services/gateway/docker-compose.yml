version: '3.8'

services:
  #  ocr-gateway:
  #    build:
  #      context: .
  #      dockerfile: Dockerfile
  #    command: bun run start:gateway
  #    ports:
  #      - "3000:3000"
  #    environment:
  #      - NODE_ENV=production
  #      - PORT=3000
  #      - DATABASE_URL=postgresql://ocr:password@postgres:5432/ocr
  #      - REDIS_URL=redis://redis:6379
  #      - MINIO_ENDPOINT=minio
  #      - MINIO_PORT=9000
  #      - MINIO_ACCESS_KEY=minioadmin
  #      - MINIO_SECRET_KEY=minioadmin
  #      - MINIO_BUCKET=images
  #      - TESSERACT_URL=http://tesseract:8080
  #      - PADDLEOCR_URL=http://paddleocr:8000
  #      - ALIGNER_URL=http://aligner:8081
  #    depends_on:
  #      - postgres
  #      - redis
  #      - minio
  #    restart: unless-stopped
  #
  #  ocr-worker:
  #    build:
  #      context: .
  #      dockerfile: Dockerfile
  #    command: bun run start:worker
  #    environment:
  #      - NODE_ENV=production
  #      - DATABASE_URL=postgresql://ocr:password@postgres:5432/ocr
  #      - REDIS_URL=redis://redis:6379
  #      - MINIO_ENDPOINT=minio
  #      - MINIO_PORT=9000
  #      - MINIO_ACCESS_KEY=minioadmin
  #      - MINIO_SECRET_KEY=minioadmin
  #      - MINIO_BUCKET=images
  #      - TESSERACT_URL=http://tesseract:8080
  #      - PADDLEOCR_URL=http://paddleocr:8000
  #      - ALIGNER_URL=http://aligner:8081
  #      - WORKER_CONCURRENCY=1
  #    depends_on:
  #      - postgres
  #      - redis
  #      - minio
  #      - tesseract
  #      - paddleocr
  #      - aligner
  #    restart: unless-stopped

  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: ocr
      POSTGRES_PASSWORD: password
      POSTGRES_DB: ocr
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - '5432:5432'
    restart: unless-stopped

  redis:
    image: redis:8-alpine
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    ports:
      - '6379:6379'
    restart: unless-stopped

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio_data:/data
    ports:
      - '9000:9000'
      - '9001:9001'
    restart: unless-stopped

  # placeholder для движков ocr (tesseract, paddleocr, aligner)
  # надо будет добавить когда будут готовы dockerfile

volumes:
  postgres_data:
  redis_data:
  minio_data:
